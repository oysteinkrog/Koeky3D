This example demonstrates the use of a framebuffer to render to a texture. It extends the SimpleCube example.
This texture is then drawn to the screen using a 2d quad.
Besides rendering the cube to a off screen texture we also render the cube to the default OpenGL frame buffer